#ifndef OS_GLOBALS_H
#define OS_GLOBALS_H

#include "types.h"

extern uint64 uart0;
extern uint64 uart0_irq;

extern uint64 plic;
extern uint64 clint;

extern uint64 virtio0;
extern uint64 virtio0_irq;

#endif // OS_GLOBALS_H
